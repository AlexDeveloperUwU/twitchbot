<!DOCTYPE html>
<head>
    <!--
        Este script es para que el OBS recargue la página web automáticamente. 
        El meta tag de refresh no funciona
    -->
    <script>
        function timedRefresh(timeoutPeriod) {
            setTimeout("location.reload(true);", timeoutPeriod);
        }
        window.onload = timedRefresh(5000);
    </script>
    <link rel="stylesheet" href="/static/styles.css">
</head>

<% if(params === "null") { %>
<h1> No hay parámetros </h1>
<% } else { %>
<h1> Hay parámetros </h1>
<% } %>